import React from 'react'

# Hello from MDX page!

Entire page is and MDX file!

Here is an interactive component, which code written in mdx file!

export const Counter = () => {
  const [count, setCount] = React.useState(0)

  return (
    <button onClick={() => setCount(i => i + 1)}>Clicked times: {count}</button>
  )
}

<Counter />


[Go back](../)
